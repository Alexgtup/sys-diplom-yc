# sys-diplom yc

диплом netology, отказоустойчивая инфраструктура в yandex cloud на terraform + ansible

статус
- vpc + 3 подсети (public, private-a, private-b)
- nat gateway + route table для private подсетей
дальше security groups, vm, alb, zabbix, elastic, kibana, filebeat, snapshots

скрины будут в папке img
